# 2026-04-23 工作日志

## 新增工作：work_plan.md 框架层实施计划

用户确认了框架层改进方案（不改 func/ 底层函数），已输出 `work_plan.md`：

**3 个 commit 顺序**：时间预算+多解收集 → HDI自适应+打分择优 → 两阶段调制+验收+实验入口

**关键工程决策**：
- RRT 多解：用时间预算替代固定迭代次数，收集所有 `cost < 1.1*c_best` 的次优解
- via 点上限：`N_C * 2`，超限在安全区降采样，危险区全量保留
- 软硬约束：用两阶段验证近似（FMP 调制合格则保留，失败则回退候选），不对外称"软硬约束"
- 验收指标：min_dist > safe_margin、kappa_p99 < 2.0 rad/m、jerk_rms < 500 m/s³
- 批量实验：30 次随机种子，输出 CSV 含 11 列指标

### Codex plan 审计报告（workbuddy_plan.md）
对 `codex_plan.md` 进行了深度技术审计，覆盖三大维度：

**实现路径审查（5项核心问题）**：
1. 硬编码容量问题：Codex 诊断正确但修复方案不完整——预分配方案 A 删除冗余 `cat(3)` 追加
2. eps 防护位置不全：漏掉 `fuzzy_clustering.m` 中的 `1./dist2`，且 eps 阈值需改为 1e-8
3. 正则化缺诊断机制：需输出 cond(A) 作为自动调参依据
4. 风险自适应 HDI 公式合理但实现脱节：需定义 risk=1/d_min、离散曲率，并加 via 点上限
5. 协方差膨胀是最大断层：`pInvCov` 不能标量乘，需对 cov 膨胀再求逆，且 `fuzzy_clustering.m` 当前输出的是 cov_i^0.5 不是协方差本身

**部署逻辑验证（4项关键遗漏）**：
- 缺 IK 可行性验证（笛卡尔→关节空间可行性）
- 缺关节空间等时间重采样
- 点碰撞检测需升级为线段-障碍物相交检测
- Gazebo 仿真需 `scenario_loader.py` 做格式转换

**实验设计评审（5项核心缺陷）**：
- 对比组需补 DMP+PF 和 ProMP
- 场景需量化参数化（密度、间距、速度）
- CPU/功耗指标无意义，替换为关节速度比、奇异点条件数
- 消融实验设计存在不公平对比问题
- 规划时间右偏需用 Wilcoxon 而非 t 检验

**时间线修正**：Codex 12 周 → 实际 16-18 周

## 关键发现
- `fuzzy_clustering.m` 第 83 行 `cov(:,:,i) = cov_i.^0.5` 输出的是标准差平方根，不是协方差矩阵。这是协方差膨胀功能的阻断性问题。
- `fuzzy_clustering.m` 第 50 行 `nIn_i = (det(cov_i)^a)*cov_i^(-1)` 输出的逆协方差包含缩放因子，Python 移植时必须处理这个细节。
- 计划书审计结论：`workbuddy_plan.md` 已写入，含 19 项逐项修正建议。
