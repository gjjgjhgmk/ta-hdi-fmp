# 2026-04-25 工作日志

## 框架代码评测

对 T1-T7 框架改动进行了系统性评测，完整读取所有新增文件并输出书面报告。

### 关键发现
1. **双实现分叉**（高优先）：`run_single_trial.m` 与 `main_core_framework.m` 内嵌版本是两套独立实现，行为一致性无保证
2. **T6 风险指标**（中优先）：`score_path.m` 用平均障碍距离倒数而非最小值，局部高风险路径可能被错选
3. **T3 回归容差过严**：`rrt_shape_mean_tol=0.8m` 对 0-100 坐标系偏小
4. **候选池时间预算偏短**：0.3s 常常只返回 1 条候选，T6 打分退化
5. **T4 曲率索引偏移一位**：危险区判断的曲率值与路径段不严格对应

### 当前状态（更新 20:08）
- S1/S2/S4/S5 已由用户落地，ALL_TESTS_PASS 6/6
- S1：main_core_framework.m 改为纯配置+分发，统一调用外部 run_single_trial.m
- S2：score_path.m 风险项改为混合版 0.7/(min+0.1)+0.3/(mean+0.1)
- S4：compare_paths_approx.m 删除冗余 len_rel
- S5：候选去重改为 cost近似+中点几何判重
- 框架当前状态：配置/RRT/打分/HDI/验收/批量全层均已稳定
- 下一步：消融实验（score_weights对比）或 ROS2 Python 移植
